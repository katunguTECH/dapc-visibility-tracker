model User {
  id          String   @id @default(cuid())
  clerkId     String   @unique // Matches Clerk's userId
  mpesaNumber String?  // Optional until they pay
  hasPaid     Boolean  @default(false)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}